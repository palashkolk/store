<h1>New product</h1>

<%= form_with model: @product do |form| %>
  <div>
    <%= form.label :name, "Name" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>bin

<%= link_to "Cancel", products_path %>
